remotes {
    packageRepo {
        host = '50.56.69.25'
        user = 'repose-dev'
        knownHosts = allowAnyHosts
        identity = file("${System.properties['user.home']}/.ssh/id_rsa")
    }
}
